import React, { Fragment } from 'react'
import Header from "../../components/Header"

const Contacts = () => {
    return (
        <Fragment>
            <Header/>
        </Fragment>
    )
}

export default Contacts